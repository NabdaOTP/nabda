{
  "name": "nabda-otp-site",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/validate-deploy.js",
    "dev": "serve . -c serve.json",
    "start": "serve . -c serve.json"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
